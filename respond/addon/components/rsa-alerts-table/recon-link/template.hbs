{{#if show}}
  <span>{{#link-to "incident.recon" (query-params endpointId=endpointId eventId=eventId selection=item.id) class="recon-link-to"}}{{item.type}}{{/link-to}}</span>
{{else}}
  <span>{{item.type}}</span>
{{/if}}
