<ul class="rsa-flash-messages">
  {{#each flashMessages.queue as |flash|}}
    {{#flash-message tagName="li" flash=flash as |component flash|}}
      <hbox align="stretch">
        <centered xs="2">
          {{rsa-icon name=flash.iconName style=flash.iconStyle size=flash.iconSize}}
        </centered>
        <vbox xs="9" justify="center" class="message">
          {{flash.message}}
        </vbox>
        <vbox xs="1" justify="start" align="center">
          {{rsa-icon name="close"}}
        </vbox>
      </hbox>
    {{/flash-message}}
  {{/each}}
</ul>
<article class="respond-engine-dummy__main {{if (and entityId entityType) 'has-entity-info'}}">
  <section fill class="respond-engine-dummy__main-body">
    {{outlet}}
  </section>
  <aside fit class="respond-engine-dummy__main-aside">
    {{#if (and entityId entityType)}}
      {{context-panel entityId=entityId entityType=entityType closePanel=(route-action 'closeContextPanel')}}
    {{/if}}
  </aside>
</article>
<div id='modalDestination'></div>
{{context-tooltip clickDataAction=(route-action 'openContextPanel')}}
