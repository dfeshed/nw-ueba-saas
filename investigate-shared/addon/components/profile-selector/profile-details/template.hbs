{{#if isEditing}}
  {{profile-selector/profile-details/profile-form
    profile=profile
    editProfile=editProfile
    columnGroups=columnGroups
    metaGroups=metaGroups
    selectedColumnGroupId=selectedColumnGroupId
    profiles=profiles
  }}
{{else}}
  {{profile-selector/profile-details/profile-view
    profile=profile
  }}
{{/if}}
