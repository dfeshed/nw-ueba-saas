package org.flume.source;

/**
 * represents the component that does the actual fetching (examples: does the actual read from mongoDB, sends the REST query, etc.)
 */
public interface SourceFetcher {
}
