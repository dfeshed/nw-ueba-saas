<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns:sec="http://www.springframework.org/schema/security"
	xmlns:context="http://www.springframework.org/schema/context" xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:oauth="http://www.springframework.org/schema/security/oauth2"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
    					http://www.springframework.org/schema/security/oauth2 http://www.springframework.org/schema/security/spring-security-oauth2.xsd
    					http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.0.xsd
                        http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-4.0.xsd">

	<bean id="impalaParser" class="fortscale.utils.impala.ImpalaParser" />
	<bean id="impalaClient" class="fortscale.utils.impala.ImpalaClient" />

	<bean id="impalaDriver" class="org.apache.hive.jdbc.HiveDriver"/>
	
	<bean id="dataSource" class="org.springframework.jdbc.datasource.SimpleDriverDataSource">
	  <constructor-arg name="driver" ref="impalaDriver"/>
	  <constructor-arg name="url" value="${impala.url}"/>
	</bean>
	
	<bean id="impalaJdbcTemplate" class="org.springframework.jdbc.core.JdbcTemplate">
	  <constructor-arg ref="dataSource"/>
	</bean>

	<bean id="cleanupPredicate" class="fortscale.utils.cleanup.CleanupPredicate" />

	<bean id="mongoUtils" class="fortscale.utils.mongodb.MongoUtil" />

	<bean id="kafkaUtils" class="fortscale.utils.kafka.KafkaUtils" />

	<bean id="impalaUtils" class="fortscale.utils.impala.ImpalaUtils" />

	<bean id="hdfsUtils" class="fortscale.utils.hdfs.HDFSUtil" />

	<bean id="storeUtils" class="fortscale.utils.store.StoreUtils" />

	<bean id="imageUtils" class="fortscale.utils.image.ImageUtils" />

	<bean id="jadeUtils" class="fortscale.utils.jade.JadeUtils" />

	<bean id="clouderaUtils" class="fortscale.utils.cloudera.ClouderaUtils" >
		<constructor-arg name="serverHost" value="${cloudera_manager_host}" />
		<constructor-arg name="clusterName" value="${cm_cluster_name}" />
		<constructor-arg name="cmAdminUser" value="${cm_user}" />
		<constructor-arg name="cmAdminPass" value="${cm_pass}" />
	</bean>
	<bean id="influxdbClient" class="fortscale.utils.influxdb.InfluxdbClient">
		<constructor-arg index="0" value="${influxdb.ip}" />
		<constructor-arg index="1" value="${influxdb.port}" />
		<constructor-arg index="2" value="${influxdb.rest.loglevel}"/>
	</bean>

</beans>