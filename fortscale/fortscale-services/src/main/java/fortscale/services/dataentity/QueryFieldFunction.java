package fortscale.services.dataentity;

/**
 * Represents a function that can be called on a field in a data query
 * @author Yossi
 *
 */
public enum QueryFieldFunction {
	count, min, max, to_date, avg, sum
}
