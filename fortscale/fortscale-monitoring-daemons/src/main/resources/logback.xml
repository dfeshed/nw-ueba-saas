<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true">

	<property name="LOG_HOME" value="/var/log/fortscale/monitoringDaemon" />

	<appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
		<encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
			<Pattern>%d [%thread] %-5level %logger{36}.%M\(%line\) - %msg%n%xEx{full}</Pattern>
		</encoder>
	</appender>

	<appender name="LogFile" class="ch.qos.logback.core.rolling.RollingFileAppender">
		<file>${BASE_LOG_DIR}/${GROUP_NAME}/${PROCESS_NAME}/${PROCESS_NAME}.log</file>
		<append>true</append>
		<rollingPolicy class="ch.qos.logback.core.rolling.FixedWindowRollingPolicy">
			<fileNamePattern>>${BASE_LOG_DIR}/${GROUP_NAME}/${PROCESS_NAME}/${PROCESS_NAME}.log.%i</fileNamePattern>
			<minIndex>1</minIndex>
			<maxIndex>5</maxIndex>
		</rollingPolicy>

		<triggeringPolicy class="ch.qos.logback.core.rolling.SizeBasedTriggeringPolicy">
			<maxFileSize>100MB</maxFileSize>
		</triggeringPolicy>
		
		<encoder>
			<Pattern>%d [%thread] %-5level %logger{36}.%M\(%line\) - %msg%n%xEx{full}</Pattern>
		</encoder>
	</appender>

	<root level="info">
		<appender-ref ref="STDOUT" />
		<appender-ref ref="LogFile" />
	</root>

	<logger name="org.quartz" additivity="false" level="warn" >
		<appender-ref ref="STDOUT" />
		<appender-ref ref="LogFile" />
	</logger>

</configuration>
