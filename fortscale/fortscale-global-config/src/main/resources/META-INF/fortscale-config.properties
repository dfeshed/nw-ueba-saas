######################################### Application flags ###########################################
fortscale.site.root.url=${fortscale.site.root.url}

######################################### DATABASE ####################################################
mongo.host.name=localhost
mongo.host.port=27017
mongo.db.name=${mongo.db.name}

impala.host=${impala.host}
impala.port=${impala.port}
impala.url=${impala.url}