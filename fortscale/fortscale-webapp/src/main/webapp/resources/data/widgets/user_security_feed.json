{
    "reportId": "user_security_feed",
    "title": "Security feed notifications",
    "noDataMessage": "No notifications found",
    "views": [
        {
            "type": "table",
            "className": "widget-table-list",
            "settings": {
                "fields": [
                    { "name": "Name",  "value": "<a href='#/d/user/{{fsId}}'>{{displayName || name}}</a>{{cause}}" }
                ]
            }
        }
    ]
}