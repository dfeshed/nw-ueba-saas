{
    "requiredParams": ["username"],
    "endpoint": {
        "query":  "SELECT substr(to_date(timegenerated),1,11) as day, count(*) as EventsCount FROM wmievents4769 WHERE lcase(account_name)=lcase('{{@username}}') and failure_code='0x0' and datediff(to_date(now()),timegenerated)<7 group by day order by day limit 7",
        "entity": "investigate"
    }
}