{
    "dataSource": "api",
    "endpoint": {
        "query":  "SELECT username, count(distinct source_ip) as failcount FROM vpndata WHERE status='FAIL' GROUP BY username having failcount>1 ORDER BY failcount desc LIMIT 10",
        "entity": "investigate"
    }
}