{
    "dataSource": "api",
    "endpoint": {
        "query":  "SELECT username,count(*) as failCount FROM vpndata WHERE status='FAIL' and date_time_unix>(unix_timestamp()-604800) GROUP BY username ORDER BY failCount  desc LIMIT 10",
        "entity": "investigate"
    }
}