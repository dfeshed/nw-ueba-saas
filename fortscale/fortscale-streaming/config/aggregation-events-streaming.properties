# Job
job.factory.class=fortscale.streaming.GracefulShutdownLocalJobFactory
job.shutdown.timeout.ms=300000
job.name=aggregation-events-streaming

# Task
task.class=fortscale.streaming.task.AggregationEventsStreamTask

# Needs to be changed to kafka.fortscale-ssh-model
task.inputs=kafka.fortscale-vpnsession-event-score,kafka.fortscale-vpn-event-score,kafka.fortscale-ssh-event-score,kafka.fortscale-4769-event-score,kafka.fortscale-amt-event-score
#  every 4 hours (4 * 60 * 60 * 1000 = 14,400,000 ms)
task.window.ms=14400000

# Fortscale specific task config parameters
fortscale.context=classpath*:META-INF/spring/streaming-context.xml
fortscale.timestamp.field=${impala.table.fields.epochtime}
fortscale.data.source.field=${impala.table.fields.data.source}

#data source configuration:
fortscale.ssh.input.topic=fortscale-ssh-computer-tagged-clustered
fortscale.4769.input.topic=fortscale-4769-event-score
fortscale.amt.input.topic=fortscale-amt-event-score
fortscale.vpn_event.input.topic=fortscale-vpn-event-score
fortscale.vpn_session.input.topic=fortscale-vpnsession-event-score

# Serializers
serializers.registry.json.class=org.apache.samza.serializers.JsonSerdeFactory
serializers.registry.string.class=org.apache.samza.serializers.StringSerdeFactory
serializers.registry.integer.class=org.apache.samza.serializers.IntegerSerdeFactory
serializers.registry.metrics.class=org.apache.samza.serializers.MetricsSnapshotSerdeFactory

# Metric report every 60 seconds to a kafka topic called metrics
metrics.reporter.snapshot.class=org.apache.samza.metrics.reporter.MetricsSnapshotReporterFactory
metrics.reporter.snapshot.stream=kafka.metrics
systems.kafka.streams.metrics.samza.msg.serde=metrics
metrics.reporters=snapshot

# Systems
systems.kafka.samza.factory=org.apache.samza.system.kafka.KafkaSystemFactory
systems.kafka.samza.msg.serde=string
systems.kafka.samza.offset.default=oldest
systems.kafka.consumer.zookeeper.connect=localhost:2181
systems.kafka.consumer.auto.offset.reset=smallest
systems.kafka.producer.metadata.broker.list=localhost:9092
systems.kafka.producer.producer.type=sync
systems.kafka.producer.retry.backoff.ms=10000
systems.kafka.producer.acks=1
systems.kafka.producer.reconnect.backoff.ms=10000
systems.kafka.producer.queue.buffering.max.ms=2000
systems.kafka.producer.batch.num.messages=1000

# Declare that we want our job's checkpoints to be enabled and written to Kafka
task.checkpoint.factory=org.apache.samza.checkpoint.kafka.KafkaCheckpointManagerFactory
task.checkpoint.replication.factor=1
task.checkpoint.system=kafka

# Key-value storage
stores.ssh-aggregation-events-streaming.factory=org.apache.samza.storage.kv.KeyValueStorageEngineFactory
stores.ssh-aggregation-events-streaming.changelog=kafka.ssh-aggregation-events-streaming-changelog
stores.ssh-aggregation-events-streaming.key.serde=string
stores.ssh-aggregation-events-streaming.msg.serde=jsonmodel
# This property is set to the number of key/value pairs that should be kept in this in-memory buffer, per task instance. The number cannot be greater than stores.*.object.cache.size.
stores.ssh-aggregation-events-streaming.write.batch.size=50
# This property determines the number of objects to keep in Samza's cache, per task instance. This same cache is also used for write buffering (see stores.*.write.batch.size). A value of 0 disables all caching and batching.
stores.ssh-aggregation-events-streaming.object.cache.size=500
# The size of LevelDB's block cache in bytes, per container. Note that this is an off-heap memory allocation, so the container's total memory use is the maximum JVM heap size plus the size of this cache.
stores.ssh-aggregation-events-streaming.container.cache.size.bytes=500000
# The amount of memory (in bytes) that LevelDB uses for buffering writes before they are written to disk
stores.ssh-aggregation-events-streaming.container.write.buffer.size.bytes=1000
