# Job
job.factory.class=org.apache.samza.job.local.LocalJobFactory
job.name=ssh-events-hdfs-write

# Task
task.class=fortscale.streaming.task.HDFSWriterStreamTask
task.inputs=kafka.fortscale-ssh-event-score
# flush the hdfs writers every 30 minutes (30*60*1000=1800000ms)
task.window.ms=1800000
#task.opts=-agentlib:jdwp=transport=dt_socket,address=localhost:9009,server=y,suspend=y

# Fortscale specific task config parameters
fortscale.timestamp.field=date_time_unix
fortscale.fields=date_time,date_time_unix,date_timeScore,username,normalized_username,status,auth_method,auth_method_score,source_ip,is_nat,normalized_src_machine,normalized_src_machine_score,target_machine,normalized_dst_machine,normalized_dst_machine_score,is_sensitive_machine,eventscore
fortscale.separator=,
fortscale.hdfs.root=/user/cloudera/processeddata/sshscores
fortscale.table.name=sshscores
fortscale.file.name=sshETL.csv
fortscale.partition.strategy=fortscale.utils.hdfs.partition.MonthlyPartitionStrategy
fortscale.split.strategy=fortscale.utils.hdfs.split.DailyFileSplitStrategy
fortscale.events.flush.threshold=25


# Serializers
serializers.registry.json.class=org.apache.samza.serializers.JsonSerdeFactory
serializers.registry.string.class=org.apache.samza.serializers.StringSerdeFactory
serializers.registry.integer.class=org.apache.samza.serializers.IntegerSerdeFactory
serializers.registry.jsonmodel.class=fortscale.streaming.serialization.PrevalanceModelSerdeFactory
serializers.registry.long.class=fortscale.streaming.serialization.LongSerdeFactory
serializers.registry.metrics.class=org.apache.samza.serializers.MetricsSnapshotSerdeFactory


# Metric report every 60 seconds to a kafka topic called metrics and as a jmx bean
metrics.reporter.snapshot.class=org.apache.samza.metrics.reporter.MetricsSnapshotReporterFactory
metrics.reporter.jmx.class=org.apache.samza.metrics.reporter.JmxReporterFactory
metrics.reporter.snapshot.stream=kafka.metrics
systems.kafka.streams.metrics.samza.msg.serde=metrics
metrics.reporters=snapshot,jmx

# Systems
systems.kafka.samza.factory=org.apache.samza.system.kafka.KafkaSystemFactory
systems.kafka.samza.msg.serde=string
systems.kafka.consumer.zookeeper.connect=localhost:2181
systems.kafka.consumer.auto.offset.reset=smallest
systems.kafka.producer.metadata.broker.list=localhost:9092
systems.kafka.producer.producer.type=syncs
# Normally, we'd set this much higher, but we want things to look snappy in the demo.
systems.kafka.producer.batch.num.messages=1

# Declare that we want our job's checkpoints to be enabled and written to Kafka
task.checkpoint.factory=org.apache.samza.checkpoint.kafka.KafkaCheckpointManagerFactory
task.checkpoint.replication.factor=1
task.checkpoint.system=kafka

# Key-value storage
stores.hdfs-write-sshscores.factory=org.apache.samza.storage.kv.KeyValueStorageEngineFactory
stores.hdfs-write-sshscores.changelog=kafka.hdfs-write-sshscores-changelog
stores.hdfs-write-sshscores.key.serde=string
stores.hdfs-write-sshscores.msg.serde=long
# Normally, we'd set this much higher, but we want things to look snappy in the demo.
stores.hdfs-write-sshscores.write.batch.size=0
stores.hdfs-write-sshscores.object.cache.size=0