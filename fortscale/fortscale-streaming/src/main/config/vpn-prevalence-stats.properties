# Job
job.factory.class=org.apache.samza.job.local.LocalJobFactory
job.name=vpn-prevalence-stats

# Task
task.class=fortscale.streaming.task.VPNEventsModelStreamTask
task.inputs=kafka.fortscale-vpn-structured
# export the state to mongodb every 30 minutes (30*60*1000=1800000ms)
task.window.ms=1800000
#task.opts=-agentlib:jdwp=transport=dt_socket,address=localhost:9009,server=y,suspend=y

# Fortscale specific task config parameters
fortscale.username.field=normalized_username
fortscale.timestamp.field=date_time_unix
fortscale.store.name=vpn-prevalence-stats
fortscale.model.name=vpn
fortscale.fields.country.model=fortscale.streaming.model.field.DiscreetValuesModel
fortscale.fields.hostname.model=fortscale.streaming.model.field.StringCaseInsensitiveValuesModel
#fortscale.fields.date_time_unix.model=fortscale.streaming.model.field.DiscreetValuesModel

# Serializers
serializers.registry.json.class=org.apache.samza.serializers.JsonSerdeFactory
serializers.registry.string.class=org.apache.samza.serializers.StringSerdeFactory
serializers.registry.integer.class=org.apache.samza.serializers.IntegerSerdeFactory
serializers.registry.jsonmodel.class=fortscale.streaming.serialization.PrevalanceModelSerdeFactory


# Systems
systems.kafka.samza.factory=org.apache.samza.system.kafka.KafkaSystemFactory
systems.kafka.samza.msg.serde=string
systems.kafka.consumer.zookeeper.connect=localhost:2181
systems.kafka.consumer.auto.offset.reset=largest
systems.kafka.producer.metadata.broker.list=localhost:9092
systems.kafka.producer.producer.type=sync
# Normally, we'd set this much higher, but we want things to look snappy in the demo.
systems.kafka.producer.batch.num.messages=1


# Key-value storage
stores.vpn-prevalence-stats.factory=org.apache.samza.storage.kv.KeyValueStorageEngineFactory
stores.vpn-prevalence-stats.changelog=kafka.vpn-prevalence-stats-changelog
stores.vpn-prevalence-stats.key.serde=string
stores.vpn-prevalence-stats.msg.serde=jsonmodel
# Normally, we'd set this much higher, but we want things to look snappy in the demo.
stores.vpn-prevalence-stats.write.batch.size=0
stores.vpn-prevalence-stats.object.cache.size=0