import Ember from 'ember';
import layout from '../templates/components/rsa-content-definition';

const { Component } = Ember;

export default Component.extend({

  layout,

  classNames: ['rsa-content-definition']

});