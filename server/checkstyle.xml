<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
        "-//Puppy Crawl//DTD Check Configuration 1.3//EN"
        "http://www.puppycrawl.com/dtds/configuration_1_3.dtd">

<!--

    Security Analytics UI - Java Coding Guidelines
    https://wiki.na.rsa.net/display/NextGenWeb/Java+Guidelines

 -->

<module name="Checker">
    <property name="charset" value="UTF-8"/>

    <property name="severity" value="error"/>

    <property name="fileExtensions" value="java, properties, xml"/>

    <!-- 1.3 Whitespace characters -->
    <module name="FileTabCharacter">
        <property name="eachLine" value="true"/>
    </module>

    <module name="TreeWalker">
        <!-- 1.4 Special escape sequences  -->
        <module name="IllegalTokenText">
            <property name="tokens" value="STRING_LITERAL, CHAR_LITERAL"/>
            <property name="format"
                      value="\\u00(08|09|0(a|A)|0(c|C)|0(d|D)|22|27|5(C|c))|\\(0(10|11|12|14|15|42|47)|134)"/>
            <property name="message" value="Avoid using corresponding octal or Unicode escape."/>
        </module>

        <!-- 1.5 Non-ASCII characters -->
        <module name="AvoidEscapedUnicodeCharacters">
            <property name="allowEscapesForControlCharacters" value="true"/>
            <property name="allowByTailComment" value="true"/>
            <property name="allowNonPrintableEscapes" value="true"/>
        </module>

        <!-- 2.1, 2.2 Package and imports -->
        <module name="EmptyLineSeparator">
            <property name="allowNoEmptyLineBetweenFields" value="true"/>
        </module>

        <!-- 2.1, 2.2.2 No package or import line wraps -->
        <module name="NoLineWrap"/>

        <!-- 2.2.1 No wildcard imports -->
        <module name="AvoidStarImport"/>

        <!-- 2.3.1 Exactly one top-level class declaration -->
        <module name="OneTopLevelClass"/>

        <!-- 2.3.1 Exactly one top-level class declaration -->
        <module name="OuterTypeFilename"/>

        <!-- 2.3.2.1 Overloads: never split -->
        <module name="OverloadMethodsDeclarationOrder"/>

        <!-- 2.5 Method length -->
        <module name="MethodLength">
            <property name="max" value="150"/>
        </module>

        <!-- 2.7 Cyclomatic complexity -->
        <module name="CyclomaticComplexity">
            <property name="max" value="12"/>
        </module>

        <!-- 2.8 Method parameter count -->
        <module name="ParameterNumber">
            <property name="max" value="7"/>
        </module>

        <!-- 3.1.1 Braces are used where optional -->
        <module name="NeedBraces"/>

        <!-- 3.1.2 Nonempty blocks -->
        <module name="LeftCurly">
            <property name="maxLineLength" value="120"/>
        </module>

        <!-- 3.1.2 Nonempty blocks -->
        <module name="RightCurly"/>

        <!-- 3.1.2 Nonempty blocks -->
        <module name="RightCurly">
            <property name="option" value="alone"/>
            <property name="tokens"
                      value="CLASS_DEF, METHOD_DEF, CTOR_DEF, LITERAL_FOR, LITERAL_WHILE, LITERAL_DO, STATIC_INIT, INSTANCE_INIT"/>
        </module>

        <!-- 3.1.3 Empty blocks: may be concise -->
        <module name="EmptyBlock">
            <property name="option" value="TEXT"/>
            <property name="tokens" value="LITERAL_TRY, LITERAL_FINALLY, LITERAL_IF, LITERAL_ELSE, LITERAL_SWITCH"/>
        </module>

        <!-- 3.2.1, 3.2.2 Block indentation -->
        <module name="Indentation">
            <property name="basicOffset" value="4"/>
            <property name="braceAdjustment" value="0"/>
            <property name="caseIndent" value="4"/>
            <property name="throwsIndent" value="4"/>
            <property name="lineWrappingIndentation" value="8"/>
            <property name="arrayInitIndent" value="4"/>
        </module>

        <!-- 3.3 One statement per line -->
        <module name="OneStatementPerLine"/>

        <!-- 3.4 Column limit: 120 characters -->
        <module name="LineLength">
            <property name="max" value="120"/>
            <property name="ignorePattern" value="^package.*|^import.*|a href|href|http://|https://|ftp://"/>
        </module>

        <!-- 3.4 Column limit: 120 characters -->
        <module name="OperatorWrap">
            <property name="option" value="NL"/>
            <property name="tokens"
                      value="BAND, BOR, BSR, BXOR, DIV, EQUAL, GE, GT, LAND, LE, LITERAL_INSTANCEOF, LOR, LT, MINUS, MOD, NOT_EQUAL, PLUS, QUESTION, SL, SR, STAR "/>
        </module>

        <!-- 3.5.2 Horizontal whitespace -->
        <module name="MethodParamPad"/>

        <!-- 3.5.2 Horizontal whitespace -->
        <module name="WhitespaceAround">
            <property name="allowEmptyConstructors" value="true"/>
            <property name="allowEmptyMethods" value="true"/>
            <property name="allowEmptyTypes" value="true"/>
            <property name="allowEmptyLoops" value="true"/>
            <message key="ws.notFollowed"
                     value="WhitespaceAround: ''{0}'' is not followed by whitespace. Empty blocks may only be represented as '{}' when not part of a multi-block statement (4.1.3)"/>
            <message key="ws.notPreceded"
                     value="WhitespaceAround: ''{0}'' is not preceded with whitespace."/>
        </module>

        <!-- 3.5.2 Horizontal whitespace -->
        <module name="GenericWhitespace">
            <message key="ws.followed"
                     value="GenericWhitespace ''{0}'' is followed by whitespace."/>
            <message key="ws.preceded"
                     value="GenericWhitespace ''{0}'' is preceded with whitespace."/>
            <message key="ws.illegalFollow"
                     value="GenericWhitespace ''{0}'' should followed by whitespace."/>
            <message key="ws.notPreceded"
                     value="GenericWhitespace ''{0}'' is not preceded with whitespace."/>
        </module>

        <!-- 3.7.1.1 One variable per declaraction -->
        <module name="MultipleVariableDeclarations"/>

        <!-- 3.7.1.2 Declared when needed, initialized as soon as possible -->
        <module name="VariableDeclarationUsageDistance"/>

        <!-- 3.7.2 No C-style array declarations -->
        <module name="ArrayTypeStyle"/>

        <!-- 3.7.3.2 The default cause is present -->
        <module name="MissingSwitchDefault"/>

        <!-- 3.7.3.1 Fall-through: commented -->
        <module name="FallThrough"/>

        <!-- 3.8 Annotations -->
        <module name="AnnotationLocation">
            <property name="tokens" value="CLASS_DEF, INTERFACE_DEF, ENUM_DEF, METHOD_DEF, CTOR_DEF, VARIABLE_DEF"/>
        </module>

        <!-- 3.9 Modifiers -->
        <module name="ModifierOrder"/>

        <!-- 3.10 Numeric Literals -->
        <module name="UpperEll"/>

        <!-- 4.1 Package names -->
        <module name="PackageName">
            <property name="format" value="^[a-z]+(\.[a-z][a-z0-9]*)*$"/>
            <message key="name.invalidPattern"
                     value="Package name ''{0}'' must match pattern ''{1}''."/>
        </module>

        <!-- 4.2 Class names -->
        <module name="TypeName">
            <message key="name.invalidPattern"
                     value="Type name ''{0}'' must match pattern ''{1}''."/>
        </module>

        <!-- 4.3 Method names -->
        <module name="MethodName">
            <property name="format" value="^[a-z][a-z0-9][a-zA-Z0-9_]*$"/>
            <message key="name.invalidPattern"
                     value="Method name ''{0}'' must match pattern ''{1}''."/>
        </module>

        <!-- 4.5 Non-constant field names -->
        <module name="MemberName">
            <property name="format" value="^[a-z][a-z0-9][a-zA-Z0-9]*$"/>
            <message key="name.invalidPattern"
                     value="Member name ''{0}'' must match pattern ''{1}''."/>
        </module>

        <!-- 4.6 Parameter names -->
        <module name="ParameterName">
            <property name="format" value="^[a-z][a-z0-9][a-zA-Z0-9]*$"/>
            <message key="name.invalidPattern"
                     value="Parameter name ''{0}'' must match pattern ''{1}''."/>
        </module>

        <!-- 4.7 Local variable names -->
        <module name="LocalVariableName">
            <property name="tokens" value="VARIABLE_DEF"/>
            <property name="format" value="^[a-z][a-z0-9][a-zA-Z0-9]*$"/>
            <property name="allowOneCharVarInForLoop" value="true"/>
            <message key="name.invalidPattern"
                     value="Local variable name ''{0}'' must match pattern ''{1}''."/>
        </module>

        <!-- 4.8 Type variable names -->
        <module name="ClassTypeParameterName">
            <property name="format" value="(^[A-Z][0-9]?)$|([A-Z][a-zA-Z0-9]*[T]$)"/>
            <message key="name.invalidPattern"
                     value="Class type name ''{0}'' must match pattern ''{1}''."/>
        </module>

        <!-- 4.8 Type variable names -->
        <module name="MethodTypeParameterName">
            <property name="format" value="(^[A-Z][0-9]?)$|([A-Z][a-zA-Z0-9]*[T]$)"/>
            <message key="name.invalidPattern"
                     value="Method type name ''{0}'' must match pattern ''{1}''."/>
        </module>

        <!-- 4.9 Camel case: defined -->
        <module name="AbbreviationAsWordInName">
            <property name="ignoreFinal" value="false"/>
            <property name="allowedAbbreviationLength" value="1"/>
        </module>

        <!-- 5.2 Caught exceptions: not ignored -->
        <module name="EmptyCatchBlock">
            <property name="exceptionVariableName" value="expected"/>
        </module>

        <!-- 5.9 Class Comment -->
        <module name="NonEmptyAtclauseDescription"/>

        <!-- 5.9 Class Comment -->
        <module name="JavadocType">
            <property name="scope" value="public"/>
        </module>

        <!-- 5.11 Finalizers: not used -->
        <module name="NoFinalizer"/>
    </module>
</module>
