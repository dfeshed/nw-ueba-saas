{{recon-event-header index=index total=total }}

<hbox>
  {{recon-event-content}}

  {{#if isMetaShown}}
    <vbox xs="4" class="event-content-box">
      {{recon-meta-content}}
    </vbox>
  {{/if}}
</hbox>
