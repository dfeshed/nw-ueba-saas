<div class="email-body-text">
  <div class="emailContent">
    <iframe id="{{emailRenderId}}" srcdoc="{{renderEmailBodyContent}}" width="100%" height="100%"></iframe>
  </div>
  {{#if displayShowMoreButton}}
    <div class="email-show-more">
      {{#rsa-form-button defaultAction= (action 'showRemainingEmailContent')}}
        {{t 'recon.emailView.showMore' }}
      {{/rsa-form-button}}
    </div>
  {{/if}}
</div>
