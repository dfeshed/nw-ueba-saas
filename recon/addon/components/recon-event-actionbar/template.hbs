<grid>
  <hbox class="dropdowns">
    {{#if isPacketView }}
      {{#rsa-form-button isDisabled=true}}Export PCAP{{/rsa-form-button}}
    {{/if}}
    {{#if isFileView }}
      {{recon-event-actionbar/export-files}}
    {{/if}}
    {{#if isTextView }}
      {{recon-event-actionbar/export-logs}}
      {{#unless isLogEvent}}
        {{recon-event-actionbar/decode-text}}
      {{/unless}}
    {{/if}}
  </hbox>
</grid>
