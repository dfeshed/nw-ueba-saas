{{#if (and eventIndex eventTotal)}}
  <div class="event-info">
    <span class="lighter">{{formattedEventIndex}} of {{formattedEventTotal}}</span>
    events
  </div>
{{/if}}

{{#if isPacket}}
  <hbox class="packet-pagination">
    {{recon-pager/data-pagination}}
  </hbox>
{{/if}}

{{#if isText}}
  {{recon-pager/text-pagination}}
{{/if}}
