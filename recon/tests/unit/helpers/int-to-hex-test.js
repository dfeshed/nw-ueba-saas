import { intToHex } from 'dummy/helpers/int-to-hex';
import { module, test } from 'qunit';

module('Unit | Helper | int to hex');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = intToHex([42]);
  assert.ok(result);
});
