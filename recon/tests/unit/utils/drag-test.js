// import drag from 'dummy/utils/drag';
import { module, test } from 'qunit';

module('Unit | Utility | drag');

// Replace this with your real tests.
test('it works', function(assert) {
  assert.expect(0);
  // let result = drag();
  // assert.ok(result);
});
