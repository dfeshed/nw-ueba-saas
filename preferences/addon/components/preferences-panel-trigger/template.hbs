{{rsa-icon name='settings-1'
  click=(action 'togglePreferencesPanel' additionalFilters preferencesConfig)
  title=(t (concat 'preferences.' preferencesConfig.modelName '.triggerTip'))
  active=isExpanded}}
