<h2 class="title-header">
  {{#rsa-form-button class='close-zone' isIconOnly=true click=openFilterPanel }}
    {{rsa-icon name='filter-2'}}
  {{/rsa-form-button}}
</h2>
<hbox class="toolbar-actions">
  {{service-selector services=servers serviceId=serverId onServiceSelection=(action 'setEndpointServer')}}
  {{files-toolbar/export-button}}
  {{#rsa-form-button class="view-certificate-button" click=(action 'toggleCertificateView')}}
    {{t 'investigateFiles.button.viewCertificates'}}
  {{/rsa-form-button}}  
  <div class="divider"/>
  {{endpoint/file-actionbar
    onSaveFileStatus = (action 'saveFileStatus' checksums)
    getSavedFileStatus = (action 'getSavedFileStatus')
    resetRiskScore = (action 'resetRiskScoreAction')
    serviceList=serviceList
    metaName='checksumSha256'
    itemList=itemList
    fileStatusData=data
    selectedFileCount=selectedFileCount
    getAllServices=(action 'getAllServices')
    isRemediationAllowed=remediationStatus
    retrieveRemediationStatus=(action 'retrieveRemediationStatus')}}
</hbox>