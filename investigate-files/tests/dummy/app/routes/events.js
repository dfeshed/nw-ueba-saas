import Route from 'ember-route';

/**
 * @class events route
 * This route is a required dependency for the investigate-files engine.
 * Without it, the consuming app (dummy) won't be able to mount the engine
 * routes.
 * @public
 */
export default Route;