{{#if model.code}}
  {{hbs-example class="spec-body-member__example"
    title="Example"
    code=model.code.hbs
    dataType=model.dataType
  }}
{{else}}
  {{#each model.categories as |category|}}
    {{spec-body-category model=category spec=model}}
  {{/each}}
{{/if}}
