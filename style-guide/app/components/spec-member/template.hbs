{{#if (or title subtitle)}}
  <header>
    {{#if title}}
      <h4 class="title">{{title}}</h4>
    {{/if}}
  
    {{#if subtitle}}
      <p class="subtitle">{{subtitle}}</p>
    {{/if}}  
  </header>
{{/if}}

<section>
  {{yield}}  
</section>
