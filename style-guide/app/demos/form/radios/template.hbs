<hbox fit>
  {{spec-masthead model=model}}
</hbox>

<hbox>
  <fill block class="scroll-box">
  {{#spec-category title="States"}}
    {{#spec-member title="Default ... (original - NOT accessible)"}}
      <div class='rsa-form-radio-group'>
        <h4 class='rsa-form-radio-group-label'>Radio Group Label</h4>
        <label class="rsa-form-radio-label {{if (eq 'foo' valueStore) 'checked'}}">
          {{radio-button
            value="foo"
            groupValue=valueStore
            changed="valueChanged"}}
            Foo
        </label>

        <label class="rsa-form-radio-label {{if (eq 'bar' valueStore) 'checked'}}">
          {{radio-button
            value="bar"
            groupValue=valueStore
            changed="valueChanged"}}
            Bar
        </label>
      </div>

      {{!-- BEGIN-SNIPPET radios1
      <div class='rsa-form-radio-group'>
        <h4 class='rsa-form-radio-group-label'>Radio Group Label</h4>
        <label class="rsa-form-radio-label {{if (eq 'foo' valueStore) 'checked'}}">
          {{radio-button
            value="foo"
            groupValue=valueStore
            changed="valueChanged"}}
            Foo
        </label>

        <label class="rsa-form-radio-label {{if (eq 'bar' valueStore) 'checked'}}">
          {{radio-button
            value="bar"
            groupValue=valueStore
            changed="valueChanged"}}
            Bar
        </label>
      </div>
      END-SNIPPET --}}
      {{code-snippet name="radios1.hbs"}}
    {{/spec-member}}

    {{#spec-member title="Default ... (new - IS accessible)"}}
      <div class='rsa-form-radio-group'>
        <h4 class='rsa-form-radio-group-label'>Radio Group Label</h4>
          {{#radio-button
            classNames='rsa-form-radio-wrapper'
            value='foo'
            groupValue=valueStore
            name='defaultRadioGroup'
            changed='valueChanged'
          }}
            <span>Foo</span>
          {{/radio-button}}

          {{#radio-button
            classNames='rsa-form-radio-wrapper'
            value='bar'
            groupValue=valueStore
            name='defaultRadioGroup'
            changed='valueChanged'
          }}
            <span>Bar</span>
          {{/radio-button}}
      </div>

      {{!-- BEGIN-SNIPPET radios1a
      <div class='rsa-form-radio-group'>
        <h4 class='rsa-form-radio-group-label'>Radio Group Label</h4>
          {{#radio-button
            classNames='rsa-form-radio-wrapper'
            value='foo'
            groupValue=valueStore
            name='defaultRadioGroup'
            changed='valueChanged'
          }}
            <span>Foo</span>
          {{/radio-button}}

          {{#radio-button
            classNames='rsa-form-radio-wrapper'
            value='bar'
            groupValue=valueStore
            name='defaultRadioGroup'
            changed='valueChanged'
          }}
            <span>Bar</span>
          {{/radio-button}}
      </div>
      END-SNIPPET --}}
      {{code-snippet name="radios1a.hbs"}}
    {{/spec-member}}

    {{#spec-member title="Disabled ... (original - NOT accessible)"}}
      <div class='rsa-form-radio-group'>
        <h4 class='rsa-form-radio-group-label'>Radio Group Label</h4>
        <label class="rsa-form-radio-label {{if true 'disabled'}} {{if (eq 'foo' valueStore) 'checked'}}">
          {{radio-button
            disabled=true
            value="foo"
            groupValue=valueStore
            changed="valueChanged"}}
            Foo
        </label>

        <label class="rsa-form-radio-label {{if (eq 'bar' valueStore) 'checked'}}">
          {{radio-button
            value="bar"
            groupValue=valueStore
            changed="valueChanged"}}
            Bar
        </label>
      </div>
      {{!-- BEGIN-SNIPPET radios3
      <div class='rsa-form-radio-group'>
        <h4 class='rsa-form-radio-group-label'>Radio Group Label</h4>
        <label class="rsa-form-radio-label {{if true 'disabled'}} {{if (eq 'foo' valueStore) 'checked'}}">
          {{radio-button
            disabled=true
            value="foo"
            groupValue=valueStore
            changed="valueChanged"}}
            Foo
        </label>

        <label class="rsa-form-radio-label {{if (eq 'bar' valueStore) 'checked'}}">
          {{radio-button
            value="bar"
            groupValue=valueStore
            changed="valueChanged"}}
            Bar
        </label>
      </div>
      END-SNIPPET --}}
      {{code-snippet name="radios3.hbs"}}
    {{/spec-member}}

    {{#spec-member title="Disabled ... (new - IS accessible)"}}
      <div class='rsa-form-radio-group'>
        <h4 class='rsa-form-radio-group-label'>Radio Group Label</h4>
          {{#radio-button
            classNames=(concat 'rsa-form-radio-wrapper' (if true ' disabled'))
            disabled=(if true true)
            value='foo'
            groupValue=valueStore
            name='disabledRadioGroup'
            changed='valueChanged'
          }}
            <span>Foo</span>
          {{/radio-button}}

          {{#radio-button
            classNames='rsa-form-radio-wrapper'
            value='bar'
            groupValue=valueStore
            name='disabledRadioGroup'
            changed='valueChanged'
          }}
            <span>Bar</span>
          {{/radio-button}}
      </div>

      {{!-- BEGIN-SNIPPET radios3a
      <div class='rsa-form-radio-group'>
        <h4 class='rsa-form-radio-group-label'>Radio Group Label</h4>
          {{#radio-button
            classNames=(concat 'rsa-form-radio-wrapper' (if true ' disabled'))
            disabled=(if true true)
            value='foo'
            groupValue=valueStore
            name='disabledRadioGroup'
            changed='valueChanged'
          }}
            <span>Foo</span>
          {{/radio-button}}

          {{#radio-button
            classNames='rsa-form-radio-wrapper'
            value='bar'
            groupValue=valueStore
            name='disabledRadioGroup'
            changed='valueChanged'
          }}
            <span>Bar</span>
          {{/radio-button}}
      </div>
      END-SNIPPET --}}
      {{code-snippet name="radios3a.hbs"}}
    {{/spec-member}}

    {{#spec-member title="Error ... (original - NOT accessible)"}}
      <div class='rsa-form-radio-group'>
        <h4 class='rsa-form-radio-group-label'>Radio Group Label</h4>
        <label class="rsa-form-radio-label {{if true 'error'}} {{if (eq 'foo' valueStore) 'checked'}}">
          {{radio-button
            value="foo"
            groupValue=valueStore
            changed="valueChanged"}}
            Foo
        </label>

        <label class="rsa-form-radio-label {{if (eq 'bar' valueStore) 'checked'}}">
          {{radio-button
            value="bar"
            groupValue=valueStore
            changed="valueChanged"}}
            Bar
        </label>
      </div>
      {{!-- BEGIN-SNIPPET radios4
      <div class='rsa-form-radio-group'>
        <h4 class='rsa-form-radio-group-label'>Radio Group Label</h4>
        <label class="rsa-form-radio-label  {{if true 'error'}} {{if (eq 'foo' valueStore) 'checked'}}">
          {{radio-button
            value="foo"
            groupValue=valueStore
            changed="valueChanged"}}
            Foo
        </label>

        <label class="rsa-form-radio-label {{if (eq 'bar' valueStore) 'checked'}}">
          {{radio-button
            value="bar"
            groupValue=valueStore
            changed="valueChanged"}}
            Bar
        </label>
      </div>
      END-SNIPPET --}}
      {{code-snippet name="radios4.hbs"}}
    {{/spec-member}}

    {{#spec-member title="Error ... (new - IS accessible)"}}
      <div class='rsa-form-radio-group'>
        <h4 class='rsa-form-radio-group-label'>Radio Group Label</h4>
          {{#radio-button
            classNames=(concat 'rsa-form-radio-wrapper' (if true ' error'))
            value='foo'
            groupValue=valueStore
            name='errorRadioGroup'
            changed='valueChanged'
          }}
            <span>Foo</span>
          {{/radio-button}}

          {{#radio-button
            classNames='rsa-form-radio-wrapper'
            value='bar'
            groupValue=valueStore
            name='errorRadioGroup'
            changed='valueChanged'
          }}
            <span>Bar</span>
          {{/radio-button}}
      </div>

      {{!-- BEGIN-SNIPPET radios4a
      <div class='rsa-form-radio-group'>
        <h4 class='rsa-form-radio-group-label'>Radio Group Label</h4>
          {{#radio-button
            classNames=(concat 'rsa-form-radio-wrapper' (if true ' error'))
            value='foo'
            groupValue=valueStore
            name='errorRadioGroup'
            changed='valueChanged'
          }}
            <span>Foo</span>
          {{/radio-button}}

          {{#radio-button
            classNames='rsa-form-radio-wrapper'
            value='bar'
            groupValue=valueStore
            name='errorRadioGroup'
            changed='valueChanged'
          }}
            <span>Bar</span>
          {{/radio-button}}
      </div>
      END-SNIPPET --}}
      {{code-snippet name="radios4a.hbs"}}
    {{/spec-member}}
  {{/spec-category}}
</fill>
</hbox>
