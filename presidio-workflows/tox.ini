[tox]
envlist = py27
[testenv]
deps=
    -rtests/test_requirements.txt
commands=
    py.test \
        --junitxml=test-reports/result.xml
    coverage run --source presidio -m pytest -sv tests
    coverage report
    coverage html
