{{#power-select
  options=listOfValues
  searchField='name'
  searchPlaceholder=(t 'packager.searchPlaceholder')
  selected=selectedOption
  onchange=(action 'updateDropdownSelection' selectedSettingId) as |list|
}}
  {{list.name}}
{{/power-select}}