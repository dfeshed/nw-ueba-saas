import Component from '@ember/component';

const ApplyToGroupStep = Component.extend({
  tagName: 'hbox',
  classNames: ['apply-to-group-step'],

  actions: {
  }
});

export default ApplyToGroupStep;
