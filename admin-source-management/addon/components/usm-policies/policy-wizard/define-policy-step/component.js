import Component from '@ember/component';

const DefinePolicyStep = Component.extend({
  tagName: 'vbox',
  classNames: ['define-policy-step', 'scroll-box'],

  actions: {
  }
});

export default DefinePolicyStep;
