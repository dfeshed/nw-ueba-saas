import Component from '@ember/component';

const DefinePolicySourcesStep = Component.extend({
  tagName: 'hbox',
  classNames: ['define-policy-sources-step'],

  actions: {
  }
});

export default DefinePolicySourcesStep;
