{{#if permissionAllowsDownload}}
  <label class="is-small power-select">
    {{#power-select
      selected=selectedDownloadPreference
      searchEnabled=false
      triggerClass='is-small'
      disabled=isDisabled
      onchange=(action 'downloadByPreference')}}
      <span>{{downloadTitle}}</span>
    {{/power-select}}
  </label>
{{/if}}

