{{#rsa-form-button
  class='execute-query-button'
  defaultAction=(action 'cancelOrExecuteAction')
  isDisabled=isDisabled
  style='primary'}}
  {{#if showSpinner}}
    {{rsa-loader size="small" testId="queryButtonQueued"}}
  {{else if isQueryRunning}}
    {{rsa-icon name='stop-button' style='lined' testId="queryButtonExecuting"}}
  {{else}}
    {{rsa-icon name='search' style='lined' testId="queryButtonInactive"}}
  {{/if}}
{{/rsa-form-button}}