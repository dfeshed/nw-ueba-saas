{{#if isActive}}
  {{rsa-form-input
    value=(readonly pillData.complexFilterText)
    spellcheck=false
    autofocus=true
  }}
{{else}}
  {{pillData.complexFilterText}}

  {{query-container/delete-pill
    sendMessage=(action 'handleMessage')
  }}
{{/if}}