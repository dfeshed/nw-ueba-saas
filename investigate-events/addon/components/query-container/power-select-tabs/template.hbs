
<ul class="tabrow">
  <li class="meta-tab {{if (eq activePillTab "meta") "selected"}}" {{action "switchTabs" "meta"}}>
    <a href="#">META</a>
    <span class="meta-count">({{queryCounter.metaTabCount}})</span>
  </li>
  <li class="recent-queries-tab {{if (eq activePillTab "recent queries") "selected"}}" {{action "switchTabs" "recent queries"}}>
    <a href="#">RECENT QUERIES</a>
    <span class="recent-query-count">({{recentTabCount}})</span>
  </li>
</ul>