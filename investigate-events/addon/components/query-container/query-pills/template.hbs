{{!-- {{#each pillsData as | pillData index |}}
  {{query-container/query-pill
    pillData=(readonly pillData)
    sendMessage=(action 'handleMessage')
    position=index
  }}
{{/each}} --}}
{{query-container/query-pill
  isActive=true
  sendMessage=(action 'handleMessage')
  position=newPillPosition
}}