{{!-- do not render until profiles exists --}}
{{#if profiles}}
  {{profile-selector
    profiles=profiles
    helpId=helpId
    selectProfile=(action 'selectProfile')
    modelName=modelName
    listName=listName
    stateLocation=stateLocation
  }}
{{/if}}

