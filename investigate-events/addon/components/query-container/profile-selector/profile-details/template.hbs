{{#if isEditing}}
  {{query-container/profile-selector/profile-details/profile-form
    profile=profile
    editProfile=editProfile
    columnGroups=columnGroups
    metaGroups=metaGroups
    selectedColumnGroupId=selectedColumnGroupId
    profiles=profiles
  }}
{{else}}
  {{!-- Passing editProfile through forms to validate editable profiles that are readonly as of now. TODO edit profiles--}}

  {{query-container/profile-selector/profile-details/profile-view
    profile=profile
    editProfile=editProfile
  }}
{{/if}}
