{{#if _startTimeMilli}}
  {{rsa-content-datetime class="rsa-time-range__datetime start" timestamp=_startTimeMilli displayMilliseconds=false}}
  {{#if _endTimeMilli}}
    <span class="rsa-investigate-time-range__sep">-</span>
  {{/if}}
{{/if}}
{{#if _endTimeMilli}}
  {{rsa-content-datetime class="rsa-time-range__datetime end" timestamp=_endTimeMilli displayMilliseconds=false}}
{{/if}}
