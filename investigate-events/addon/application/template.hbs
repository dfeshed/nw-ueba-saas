{{#rsa-investigate-header
  activeTab='protected.investigate.investigate-events'
  as |section| }}

  {{#if (and accessControl.hasInvestigateEventsAccess section.isIconBar)}}
    {{recon-size-toggle}}
    {{events-preferences}}
  {{/if}}

  {{#if section.isMain}}
    {{outlet}}
  {{/if}}
{{/rsa-investigate-header}}

{{#if accessControl.hasInvestigateEventsAccess}}
  {{preferences-panel}}
  {{context-menu}}
{{/if}}
