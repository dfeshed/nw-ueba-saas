import domRoot from 'style-guide/utils/dom/root';
import { module, test } from 'qunit';

module('Unit | Utility | dom/root');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = domRoot();
  assert.ok(result);
});
