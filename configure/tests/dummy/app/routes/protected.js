import Route from '@ember/routing/route';

/**
 * @class protected route
 * This route is a required dependency for the respond engine. Without it, the consuming app (dummy) won't
 * be able to mount the engine routes.
 * @public
 */
export default Route;
