const LOG = 'LOG';
const NETWORK = 'NETWORK';
const META = 'META';
const FILES = 'FILES';

const CSV = 'CSV';
const JSON = 'JSON';
const XML = 'XML';
const TEXT = 'TEXT';
const TSV = 'TSV';

const PCAP = 'PCAP';
const PAYLOAD = 'PAYLOAD';
const PAYLOAD1 = 'PAYLOAD1';
const PAYLOAD2 = 'PAYLOAD2';

export const EVENT_DOWNLOAD_TYPES = {
  LOG,
  NETWORK,
  META,
  FILES
};

export const FILE_TYPES = {
  CSV,
  JSON,
  XML,
  TEXT,
  TSV,
  PCAP,
  PAYLOAD,
  PAYLOAD1,
  PAYLOAD2
};
