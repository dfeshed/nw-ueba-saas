import Component from '@ember/component';

const ReviewPolicyStep = Component.extend({
  tagName: 'hbox',
  classNames: ['review-policy-step'],

  actions: {
  }
});

export default ReviewPolicyStep;
